# Эксперименты

## 2026-07-24 — проверка готовности к запуску

- Команда: `./scripts/preflight.sh` (только чтение; транзакции не создавались).
- Результат: `status=failed`, причина — RPC balance check failed.
- Решение: live-запуск не выполнять до успешного повторного preflight.

## 2026-07-24 — повторный preflight после замены RPC

- Команда: `./scripts/preflight.sh` (только чтение; транзакции не создавались).
- Результат: успешно пройден; RPC, CLI, sender'ы и защитные пороги доступны.
- Решение: автоматический профиль технически готов, но для работы строго с одним mint требуется отдельная ручная конфигурация.

## 2026-07-24 — подготовка input для ручного профиля

- Создана закрытая резервная копия автоматических `config.toml` и `tokens.toml`.
- `tokens.toml` временно ограничен mint `FB44zC6s2jkysjaB2NC8u6XqwhPJwir1DYFzEhXbpump`; структурная проверка списка пройдена.
- Транзакции не создавались; генерация market/LUT-артефактов ожидается.

## 2026-07-24 — отказ генерации ручного market-профиля

- Команда генерации пулов для mint `FB44zC6s2jkysjaB2NC8u6XqwhPJwir1DYFzEhXbpump` завершилась до запуска: CLI получил числовое поле в float-представлении, когда ожидал `u64`.
- Ошибка воспроизведена повторно тем же read-only сетевым запросом; RPC и локальный preflight к этому отказу не относятся.
- Автоматические `config.toml` и `tokens.toml` восстановлены из закрытого snapshot; частичный `markets.toml` перенесён в snapshot для диагностики.
- Ручной профиль и live-запуск не выполнялись; транзакций не создавалось.
- Проверка после восстановления: исходный input восстановлен, `config.toml` имеет права `600`, preflight успешно пройден.

## 2026-07-24 — отказ разрешения LUT для ручных пулов

- Оператор предоставил два пула для целевого mint; они были помещены в одну ручную market-группу без изменения `config.toml`.
- Команда `get-lookup-tables` остановилась до запуска с тем же несовместимым числовым полем: внешний ответ содержит float, а CLI ожидает `u64`.
- Ручной market-файл сохранён с правами `600` в ранее созданном snapshot для продолжения после предоставления LUT или исправления resolver'а.
- Транзакций не создавалось; автоматический профиль не изменялся.

## 2026-07-24 — блокировка ручного профиля supervisor'ом

- Все предоставленные LUT подтверждены как существующие lookup-table accounts; локальная проверка ручной market-группы пройдена.
- После временного отключения auto preflight ожидаемо остановился на политике supervisor'а: `auto.enabled must be true`.
- Это локальная защитная политика, а не ошибка пулов, LUT или RPC. Изменение supervisor'а не выполнялось.
- Автоматические `config.toml` и `tokens.toml` восстановлены из закрытых копий; итоговый preflight успешно пройден. Транзакций не создавалось.

## 2026-07-24 — добавлен guarded профиль manual-single

- Supervisor теперь допускает ручный профиль только при точном совпадении утверждённых mint, двух пулов и десяти LUT в `markets.toml`; второй mint, пустой/неверный LUT и неверный пул отклоняются до preflight.
- Для default и всех A/B-профилей сохранено требование автоматического режима; sender, flashloan, static fee, ранняя остановка `0.025 SOL`, целевой лимит `0.03 SOL` и тайм-аут `300` секунд не изменены.
- Проверки: 23 unit-теста и shell syntax checks пройдены; default preflight успешно пройден. Транзакций и live-запусков не было.
- Финальное ревью заблокировало профиль: прямой Python-вызов guard обходит `--live-confirmed`, а проверка допускает дополнительный disabled market-source. Эти ограничения должны быть устранены до любого manual-single запуска.

## 2026-07-22 19:36 CEST — запуск 001

- CLI: `0.2.1`
- Режим: auto + flashloan
- Sender'ы: RPC-spam, Jito, Helius SWQOS, Circular, Falcon
- Стартовый SOL: `0.765429706`
- Конечный SOL: `0.765429706`
- Наблюдаемый расход: `0 SOL`
- Длительность: `1.076 s`
- Результат: CLI завершился с кодом `1` до начала отправки транзакций
- Причина: `[circular].tip_lamports.to` должен быть строго больше `from`
- Лог: `logs/20260722T173636Z-zavod-cli.log`
- Исправление: для всех минимальных фиксированных диапазонов установлен верхний предел `from + 1`; в preflight добавлена регрессионная проверка строгого возрастания диапазонов

### Зафиксированный профиль после исправления

- Общий dynamic fees: выключен
- RPC-spam: delay `300 ms`, priority fee `1111..3333`
- Jito: delay `1000 ms`, tip `1111..3333`
- Helius SWQOS: delay `1000 ms`, priority fee `111..333`, tip `5000..5001`
- Circular: delay `1000 ms`, priority fee `111..333`, tip `100000..100001`
- Falcon: delay `1000 ms`, priority fee `111..333`, tip `1000000..1000001`
- Early stop: `0.025 SOL`; target maximum loss: `0.03 SOL`; timeout: `300 s`
- Auto: refresh `10 s`, two mint'а принудительно, three-hop включён
- Filters: limit `2`, duration `60 s`, min tx `5`, min profit `0.2 SOL`, min profit per arb `0.01 SOL`, min ROI `0`, offchain activity учитывается
- Markets: max pools `4`, min liquidity `6 SOL`, max liquidity `780000 SOL`, route window `120 s`, half-life `20 s`, weights `0.75/0.25`

## 2026-07-22 19:38–19:44 CEST — запуск 002

- CLI: `0.2.1`
- Профиль: зафиксированный профиль после запуска 001, без дополнительных изменений
- Причина остановки: штатный тайм-аут
- Длительность: `300.705 s`
- Стартовый SOL: `0.765429706`
- SOL сразу при остановке: `0.752436312`
- SOL после расчёта транзакций в полёте: `0.752393663`
- Итоговое изменение SOL: `-0.013036043`
- wSOL до первой landed-транзакции: `0.053973545`
- wSOL после последней landed-транзакции: `0.070861244`
- Итоговое изменение wSOL: `+0.016887699`
- Совокупное изменение SOL + wSOL: `+0.003851656 SOL` эквивалента
- Средний расход SOL: `0.002601096 SOL/min`
- Совокупный результат: `+0.000768525 SOL/min` эквивалента
- Целевой лимит `0.03 SOL`: соблюдён
- Ранний порог `0.025 SOL`: не достигнут
- Процесс после остановки: отсутствует
- Лог: `logs/20260722T173858Z-zavod-cli.log`, права `600`

### Отправка и ончейн-наблюдения

- RPC: `870` принятых отправок, `434` уникальные подписи
- Jito: `131` принятый bundle, `131` уникальный bundle ID
- Helius SWQOS: `917` ответов, `130` уникальных результатов
- Circular: `131` ответ, `131` уникальная подпись
- Falcon: `1048` ответов, `130` уникальных результатов
- Публичная история кошелька за окно: `916` подтверждённых подписей
- RPC отдал полные транзакции для `664`: `422` successful, `242` failed
- Для `252` транзакций детали не получены из-за read-rate ограничений RPC; показатели successful/failed и network fee являются неполными
- На обработанной выборке network fee: `4_117_277` lamports; это нижняя граница, а не полный расход
- Изменений сторонних token balances не обнаружено; затронутые pump-token accounts остались с нулевым балансом
- `15` циклов вернули 0 mint'ов, `9` циклов — 1 mint; `force_two_mints` сработал `5` раз

### Вывод для следующей корректировки

Профиль показал положительный совокупный результат на коротком окне, но одной пятиминутной выборки недостаточно для вывода об устойчивой доходности. Следующий эксперимент должен сохранить профиль неизменным для сопоставимости и увеличить окно только при отдельном согласовании бюджета. Для оценки вклада каждого sender'а нужны раздельные A/B-прогоны; текущий общий лог доказывает доставку всех пяти каналов, но не атрибутирует landed-профит конкретному sender'у.

## 2026-07-22 19:54–19:59 CEST — запуск 003

- CLI: `0.2.1`; профиль идентичен запуску 002
- Причина остановки: штатный тайм-аут
- Длительность: `300.046 s`
- Стартовый SOL: `0.752393663`
- Конечный SOL: `0.751892390`
- Изменение SOL: `-0.000501273`
- wSOL до первой landed-транзакции: `0.070861244`
- wSOL после последней landed-транзакции: `0.072405702`
- Изменение wSOL: `+0.001544458`
- Совокупное изменение SOL + wSOL: `+0.001043185 SOL` эквивалента
- Целевой лимит и ранний порог: не достигнуты
- Процесс после остановки: отсутствует
- Лог: `logs/20260722T175404Z-zavod-cli.log`, права `600`
- Логовые признаки активности: RPC `72` отправки, Jito `11` bundle, Helius `77` ответа, Falcon `88` ответа, Circular `11` ответа
- Публичная история кошелька в окне: `78` подписей; в логах нет явных строк `profit`/`land`
- `27` циклов вернули 0 mint'ов, `1` цикл — 1 mint; `force_two_mints` сработал `1` раз

### Вывод

Два сопоставимых окна дали положительный SOL+wSOL эквивалент: запуск 002 `+0.003851656 SOL`, запуск 003 `+0.001043185 SOL`. При этом частота возможностей сильно различалась, поэтому менять комиссии на основании только этих двух окон не следует. Профиль сохраняется; следующая полезная итерация — ещё один сопоставимый прогон или отдельный A/B-тест, но не одновременная перенастройка всех sender'ов.

## 2026-07-22 20:02–20:07 CEST — запуск 004

- CLI: `0.2.1`; профиль идентичен запуску 003
- Причина остановки: штатный тайм-аут
- Длительность: `300.737 s`
- Стартовый SOL: `0.751892390`
- Конечный SOL: `0.751892390`
- Изменение SOL: `0 SOL`
- Sender-отправок: `0` по логам
- Возможности: `28` циклов с `Fetched 0 mint list`, без `profit`/`land`
- Целевой лимит и ранний порог: не достигнуты
- Процесс после остановки: отсутствует
- Лог: `logs/20260722T180243Z-zavod-cli.log`, права `600`

### Вывод

Окно 004 — контрольный нулевой прогон: бот и RPC были доступны, но за выбранные пять минут не было подходящих mint'ов, поэтому комиссия не расходовалась. Настройки не менять.

## 2026-07-22 20:08–20:13 CEST — запуск 005

- CLI: `0.2.1`; профиль идентичен запуску 004
- Причина остановки: штатный тайм-аут
- Длительность: `300.892 s`
- Стартовый SOL: `0.751892390`
- Конечный SOL: `0.745411839`
- Изменение SOL: `-0.006480551`
- wSOL до первой landed-транзакции: `0.072405702`
- wSOL после последней landed-транзакции: `0.072405702`
- Изменение wSOL: `0`
- Совокупное изменение SOL + wSOL: `-0.006480551 SOL`
- Целевой лимит `0.03 SOL`: соблюдён; ранний порог `0.025 SOL`: не достигнут
- Процесс после остановки: отсутствует
- Лог: `logs/20260722T180850Z-zavod-cli.log`, права `600`
- Логовые признаки активности: RPC `636` отправок, Jito `96` bundle, Helius `672` ответа, Falcon `768` ответа, Circular `96` ответа
- Возможности: `19` циклов с 0 mint'ов, `5` циклов с 1 mint'ом; `force_two_mints` сработал `3` раза
- Публичная история кошелька в окне: `687` подписей; token balances, кроме нулевых рабочих accounts, не изменились

### Вывод

Запуск 005 показывает, что текущий профиль может расходовать SOL на отправки без зафиксированного роста wSOL. После этого окна автоматический бесконтрольный запуск продолжать не следует: следующий шаг — A/B-изоляция sender'ов или ужесточение фильтров, с отдельным лимитом и подтверждением.

## 2026-07-22 18:30–18:35 CEST — A/B запуск 006 (без Helius SWQOS)

- Профиль: `ab-no-swqos`; RPC-spam, Jito, Circular, Falcon включены; Helius SWQOS выключен
- Остальные параметры идентичны основному профилю
- Причина остановки: штатный тайм-аут
- Длительность: `300.804 s`
- Стартовый SOL: `0.745411839`
- Конечный SOL: `0.744582024`
- Изменение SOL: `-0.000829815`
- wSOL до/после окна: `0.072405702` / `0.072405702`
- Совокупное изменение: `-0.000829815 SOL`
- Целевой лимит и ранний порог: не достигнуты
- Отправки по логу: RPC `146`, Jito `22`, Circular `22`, Falcon `176`; SWQOS `0`
- Возможности: `25` циклов с 0 mint'ов, `2` цикла с 1 mint'ом; `force_two_mints` сработал `0` раз
- Публичная история кошелька в окне: `123` подписей
- Лог: `logs/20260722T183024Z-zavod-cli.log`, права `600`

### A/B вывод

В этом окне отключение SWQOS уменьшило расход относительно запуска 005 (`0.000829815` против `0.006480551 SOL`), но профит в wSOL отсутствовал и возможностей было мало. Это подтверждает, что SWQOS дорог в периоды активности, но не доказывает причинность на доходности из-за разной рыночной активности. Основной профиль восстановлен; автоматические прогоны приостановлены до следующего решения.

## 2026-07-22 18:39–18:44 CEST — A/B запуск 007 (порог `min_profit_per_arb = 20M`, без SWQOS)

- Единственное изменение стратегии относительно основного профиля: `min_profit_per_arb` увеличен с `10000000` до `20000000` lamports
- Профиль: `ab-no-swqos`; SWQOS выключен, остальные sender'ы включены
- Причина остановки: штатный тайм-аут; длительность `300.441 s`
- Стартовый SOL: `0.744582024`; конечный SOL: `0.741499719`; изменение `-0.003082305 SOL`
- wSOL до/после окна: `0.072405702` / `0.072405702`; изменение `0`
- Совокупное изменение: `-0.003082305 SOL`; лимиты `0.03` и `0.025 SOL` не достигнуты
- Отправки: RPC `500`, Jito `76`, Circular `76`, Falcon `608`; SWQOS `0`
- Возможности: `21` циклов с 0 mint'ов, `6` циклов с 1 mint'ом; `force_two_mints` — `6`
- Публичная история кошелька в окне: `468` подписей; wSOL не изменился
- Лог: `logs/20260722T183948Z-zavod-cli.log`, права `600`

### A/B вывод

Повышение порога до 20M не улучшило результат: расход остался отрицательным, а wSOL-профит не появился. Порог восстановлен до 10M; дальше его снижать без изоляции sender'а не следует.

## 2026-07-22 18:58–19:08 CEST — sender A/B серия 008–010

- Все окна: тайм-аут 300 секунд, статические fee/tip, `min_profit_per_arb = 10M`, основной кошелёк
- 008, только RPC-spam: `0.741499719` → `0.738295180 SOL`, изменение `-0.003204539 SOL`; лог `logs/20260722T185808Z-zavod-cli.log`
- 009, только Jito: `0.738295180` → `0.738295180 SOL`, изменение `0 SOL`; лог `logs/20260722T190317Z-zavod-cli.log`
- 010, Circular + Falcon: `0.738295180` → `0.733790918 SOL`, изменение `-0.004504262 SOL`; лог `logs/20260722T190825Z-zavod-cli.log`
- Ни один тест не достиг раннего порога `0.025 SOL` или лимита `0.03 SOL`; после серии процессы отсутствуют

### A/B вывод

В этой серии Jito не показал расхода, тогда как RPC-spam и особенно связка Circular+Falcon расходовали SOL без зафиксированного профита. Это сильный сигнал для следующего теста: оставить Jito единственным активным sender'ом либо отдельно изолировать Circular и Falcon. Основной профиль не изменён.

## 2026-07-22 21:00–21:10 CEST — sender A/B серия 011–012

- 011, Circular + Falcon + RPC-spam: `0.733709363` → `0.730534030 SOL`, изменение `-0.003175333 SOL`; тайм-аут `300.222 s`; лог `logs/20260722T210033Z-zavod-cli.log`
- 012, только Jito (повтор): `0.730534030` → `0.722124772 SOL`, изменение `-0.008409258 SOL`; тайм-аут `300.714 s`; лог `logs/20260722T210538Z-zavod-cli.log`
- Ни один тест не достиг раннего порога или лимита; основной профиль не изменён

### A/B вывод

Добавление RPC-spam к Circular+Falcon не увеличило расход относительно серии 008–010. Повторный тест только Jito дал существенный расход `0.008409258 SOL`, поэтому предыдущий нулевой результат Jito нельзя считать стабильным; Jito также требует дальнейшего контроля и не должен запускаться автоматически.

## 2026-07-22 23:36–23:41 CEST — фильтр объёма, запуск 013

- Основной профиль sender'ов без изменений
- Единственное изменение: `min_volume_lamports = 150_000_000_000` (150 SOL)
- Окно: около 300 секунд, лог `logs/20260722T233628Z-zavod-cli.log`
- Отправки: `666` строк успешной отправки по логу
- Баланс: `0.722124772` → `0.713476222 SOL`, изменение примерно `-0.008648550 SOL`
- Ранний порог/лимит supervisor не сработали; основной `config.toml` не изменён

### Вывод

Порог объёма 150 SOL не остановил активность и не снизил расход в этом окне. Следующий тест должен быть без live-расходов: сначала выяснить, почему фильтр объёма допускает эти mint'ы и сколько из отправок реально landed.

## 2026-07-22 23:43–23:48 CEST — запуск 014, объём + исключение офчейн-ботов

- Активные фильтры: `min_volume_lamports = 150_000_000_000`, `ignore_offchain_bots = true`; остальные параметры основного профиля сохранены
- Причина остановки: штатный тайм-аут; длительность `300.614 s`
- SOL: `0.713476222` → `0.710736396`; изменение `-0.002739826 SOL`
- Отправки по логу: RPC `388`, Jito `59`, Helius `413`, Circular `59`, Falcon `472`
- Ранний порог и лимит потерь не достигнуты; процесс после остановки отсутствует
- Лог: `logs/20260722T234342Z-zavod-cli.log`

### Вывод

Расход ниже запуска 013 (`0.002739826` против примерно `0.008648550 SOL`), но единичное окно не доказывает причинность: активность рынка различается. Профит не зафиксирован, поэтому автоматическое продолжение по-прежнему остановлено.

## 2026-07-22 23:53–00:03 CEST — серия 015–016, `min_roi = 0.2`

- Профиль идентичен запуску 014, кроме `min_roi = 0.2`; фильтры объёма 150 SOL и исключения офчейн-ботов остались включены
- 015: `0.710736396` → `0.705955054 SOL`, `-0.004781342 SOL`, `300.733 s`, 390 строк RPC-отправок; лог `logs/20260722T235340Z-zavod-cli.log`
- 016: `0.705955054` → `0.697099673 SOL`, `-0.008855381 SOL`, `300.344 s`, 1280 строк RPC-отправок; лог `logs/20260722T235848Z-zavod-cli.log`
- Оба окна остановлены по тайм-ауту; лимиты supervisor не достигнуты; последний read-only баланс после settling: `0.696998791 SOL`

### Вывод

`min_roi = 0.2` не дал устойчивого снижения расхода и не зафиксировал профит: активность во втором окне резко выросла. Не повышать или понижать ROI дальше на основе этой серии; сначала нужна сверка landed-транзакций, их комиссий и фактических арбитражных результатов.

## 2026-07-23 12:41–12:51 CEST — запуск 017, 10 минут

- Профиль: текущий основной, включая `min_roi = 0.2`, объём ≥150 SOL и исключение офчейн-ботов
- Причина остановки: штатный тайм-аут; длительность `600.253 s`
- SOL: `0.696998791` → `0.689940686`; изменение `-0.007058105 SOL`
- Отправки: 442 строк успешной RPC-отправки по логу
- Лимит `0.03 SOL` и ранний порог `0.025 SOL` не достигнуты; процесс остановлен
- Лог: `logs/20260723T124119Z-zavod-cli.log`

### Вывод

За 10 минут расход составил `0.007058105 SOL` без зафиксированного профита. Длинное окно подтвердило, что при текущем профиле расход продолжает накапливаться; новые live-запуски приостановлены до разборки landed-транзакций, комиссий и реального PnL.

## 2026-07-23 15:28–15:33 CEST — запуск 018, CLI 0.2.2

- Первый live-запуск после обновления с CLI `0.2.2`; профиль фильтров не изменён (`min_roi = 0.2`, объём ≥150 SOL, offchain исключён)
- Причина остановки: штатный тайм-аут; `301.045 s`
- SOL: `0.689940686` → `0.687889034`; изменение `-0.002051652 SOL`
- Отправки: 292 строки успешной RPC-отправки
- Лимиты supervisor не достигнуты; процесс остановлен; лог `logs/20260723T152844Z-zavod-cli.log`

### Вывод

В этом единичном окне расход ниже предыдущих запусков, но профит не зафиксирован и рыночная активность не контролировалась. Обновление до 0.2.2 не вызвало конфигурационных ошибок; вывод о прибыльности новой версии делать рано.

## 2026-07-24 — подготовка manual-single для тестового запуска

- Целевой mint: `FB44zC6s2jkysjaB2NC8u6XqwhPJwir1DYFzEhXbpump`; ручная market-группа содержит ровно два утверждённых пула и десять ранее проверенных LUT.
- Перед изменениями создан закрытый snapshot исходных `config.toml` и `tokens.toml`; временный `tokens.toml` ограничен одним целевым mint.
- Защитные проверки пройдены: 23 unit-теста, shell syntax check, точная структурная проверка market/LUT и профильный preflight.
- Транзакции не создавались. Тестовый запуск ожидает отдельного live-подтверждения; после завершения автоматический профиль должен быть восстановлен из snapshot.

## 2026-07-24 — результат тестового manual-single запуска

- CLI 0.2.2 зациклился на сообщении об отсутствующем payer WSOL account и не зарегистрировал ни одного route/swap/arb/profit события.
- On-chain проверка 16 отправленных подписей: все finalized и успешны. Каждая транзакция содержала только idempotent `CreateAssociatedTokenAccount`; `SyncNative`, token transfer, swap и close отсутствовали.
- Итоговая дельта плательщика по этим транзакциям равна комиссиям: `-160000` lamports (`-0.00016 SOL`). Новый rent не списывался.
- Отдельная finalized-проверка показывает один уже существующий initialized WSOL token account с amount `72423992` lamports; повторные create были no-op. Рабочая гипотеза: CLI ошибочно определяет наличие WSOL ATA до каждого idempotent create.
- Оператор остановил тест. Ctrl-C завершил Python supervisor исключением, но не остановил дочерний CLI; дочерний процесс затем отдельно остановлен сигналом INT.
- Автоматический профиль и исходный список токенов восстановлены из mode-600 snapshot; default preflight пройден. До исправления двух дефектов повторный ручной запуск не выполнять.

## 2026-07-24 — Task 3: независимая read-only верификация manual-single

- Созданы mode-600 резервные копии state-файлов до обновления: `backups/state/CURRENT.md.before-task-3-20260724T143425Z` и `backups/state/EXPERIMENTS.md.before-task-3-20260724T143425Z`.
- Полный набор read-only проверок пройден: 39 unit-тестов, `bash -n`, `py_compile`, primary CLI `0.2.2` и isolated legacy CLI `0.2.1` с `--version`/`--help`, а также default preflight. Подтверждены лимит `0.03 SOL`, early stop `0.025 SOL` и timeout `300 s`.
- Структурно подтверждено: default выбирает основной `0.2.2`, а `manual-single` — только isolated `0.2.1`; основной бинарник байт-в-байт совпадает с comparison-копией и остаётся бинарником автоматических профилей. `auto.enabled = true`, `markets_file` отсутствует, список содержит два токена, процессов `zavod-mev-bot-rust-version-cli run` нет.
- Регрессионные тесты подтверждают, что точная последовательность WSOL `missing → created → missing` вызывает `wsol_ata_loop` и shutdown process group; KeyboardInterrupt cleanup и signal escalation также покрыты.
- Ни одна transaction-capable команда и ни один live-тест не запускались. Legacy `0.2.1` не доказан как успешно проходящий WSOL startup; для этого требуется отдельный явно одобренный transaction-capable тест. Ручной live-тест остаётся заблокирован до свежего явного одобрения.

## 2026-07-24 — final-review fix wave для manual-single

- TDD RED воспроизвёл все обязательные дефекты: несовпадение реального регистра сообщений CLI 0.2.1, отсутствие streaming-redaction/output-pump, неполное владение process group, позднюю установку SIGTERM handler, нестрогий version parser и нулевой status при `cleanup_failed`.
- GREEN: 55 unit-тестов успешно пройдены. Harmless real-process fixtures подтверждают SIGINT → SIGTERM → SIGKILL, устойчивость к повторным KeyboardInterrupt, очистку surviving descendant после выхода leader, cleanup сразу после spawn и финальное отсутствие PGID. Заблокированный balance reader не задерживает WSOL SIGINT.
- Child stdout теперь поступает только через pipe; детектор получает данные сразу, а mode-600 лог получает только потоково отредактированный текст с буфером через границы chunks. Тест с разделённым между chunks секретом подтверждает отсутствие сырого значения в сохранённом логе.
- Проверены все accepted non-manual selectors на основной CLI 0.2.2, mocked launch-path `manual-single` на isolated CLI 0.2.1 и version parsing через `fullmatch` точной version-line. Безопасный fixture-preflight `manual-single` проходит с 0.2.1.
- Полная read-only проверка: shell syntax, `py_compile`, обе версии/help, default preflight, byte-identity primary 0.2.2 с comparison-копией и isolated 0.2.1 с официальным архивом, отсутствие CLI `run`-процесса. Live workspace manual-preflight ожидаемо fail-closed, пока восстановленный автоматический конфиг не переключён контролируемо.
- Конфиги, токены, wallet и бинарники не изменялись. Transaction-capable команда и live-тест не запускались; следующий manual-run требует свежего явного одобрения непосредственно перед guarded-командой.

## 2026-07-24 — final re-review: asynchronous finalization и output fail-closed

- Созданы свежие mode-600 backups source, tests, state и отчёта с stamp `20260724T151743Z` до любых правок второй волны.
- Focused RED: 5 тестов, 4 failures и 1 coverage-only pass. Подтверждены бесконечный KeyboardInterrupt retry, небезопасная финализация под реальными SIGINT/SIGTERM, отсутствие немедленного сигнала при output-pump failure и нулевой CLI status для `output_error`.
- Финализация теперь bounded: SIGINT/SIGTERM блокируются на весь cleanup, оба handlers заменяются non-raising latch handlers, pending signals ограниченно потребляются до unmask, а исходные handlers восстанавливаются только после process-group verification, pump drain и log flush/close. Исчерпание retry budget переводит результат в `cleanup_failed`.
- Реальный subprocess-тест семь секунд посылает чередующиеся SIGINT/SIGTERM во время настоящих SIGINT- и SIGTERM-фаз cleanup harmless child group. Проверка дважды подряд и в affected/full suite завершилась без traceback, в пределах 15 секунд и без PGID.
- Output-pump failure атомарно устанавливает `output_error_event` и сразу сигналит child PGID. Тест с заблокированным balance reader подтверждает сигнал до возврата balance, стабильный `output_error`, отсутствие сырого разделённого секрета в mode-600 persisted log и ненулевой CLI status.
- Safe manual preflight использует symlink на фактический isolated executable, предварительно сравненный byte-for-byte с членом `v0.2.1.zip`; config/pubkey/balance/disk зависимости инъецированы, RPC и transaction path не используются.
- GREEN: focused проверки, 33 affected tests и полный suite из 60 tests пройдены. Также пройдены `bash -n`, `py_compile`, byte-identity обоих бинарников, обе версии/help, default preflight и process-absence check.
- Конфиги, tokens, wallet, архив и бинарники не изменялись. Ни одна Zavod CLI `run` или иная transaction-capable команда не выполнялась.

## 2026-07-24 — guarded auto-search тест после исправления supervisor

- Запущен один автоматический guarded-тест CLI 0.2.2 после явного подтверждения оператора.
- Результат: `reason=wsol_ata_loop`, длительность `16.654 s`, observed SOL loss `10000` lamports; лимиты 0.025/0.03 SOL не приближались.
- Лог содержит одну последовательность создания payer WSOL ATA и немедленный повторный `missing`; guard остановил process group до второй транзакции.
- Единственная подпись finalized без ошибки. Swap/route/arb/profit событий нет, поэтому автопоиск не перешёл к рабочему pipeline.
- Process-absence check пройден; mode-600 лог сохранён. Конфигурация остаётся автоматической.
- Вывод: supervisor и WSOL fail-closed защита работают в live-условиях, но дефект определения существующего WSOL ATA присутствует и в автоматическом CLI 0.2.2. Повторный запуск без отдельного решения не выполнять.

## 2026-07-24 — откат guard к контрольной точке до WSOL-fix

- По явному указанию оператора `scripts/zavod_guard.py` и `tests/test_zavod_guard.py` восстановлены побайтно из `backups/task-1-manual-single-legacy/*.before`.
- Перед откатом текущие версии guard, теста и state-файлов сохранены в mode-600 backups.
- Безопасная проверка после отката: 23 unit-теста успешно пройдены, Python compilation и CLI help успешны, основной CLI 0.2.2 совпадает с comparison-копией, активный процесс отсутствует.
- `config.toml`, `tokens.toml`, wallet и бинарники не изменялись. Transaction-capable команды не выполнялись.
- Откат сознательно удалил WSOL-loop fail-closed detection, streaming redaction и усиленную очистку process group; следующий live-запуск требует свежего явного подтверждения и несёт риск повторения WSOL ATA цикла.

## 2026-07-24 — авторежим после отката guard

- После свежего явного подтверждения выполнен ровно один запуск `./scripts/run-guarded.sh --live-confirmed`.
- Guard завершил тест по штатному тайм-ауту: `300.17 s`, child exit `-2`.
- Баланс guard: `687589034 → 687379034` lamports; наблюдаемая дельта `-210000` lamports (`-0.00021 SOL`). Early stop `0.025 SOL` и loss limit `0.03 SOL` не достигнуты.
- Mode-600 лог `logs/20260724T162911Z-zavod-cli.log`: `21` строки WSOL missing, `20` строки WSOL created, `0` route, `0` swap, `0` arb, `0` profit и `0` error/failed по агрегированному поиску.
- Активный CLI-процесс после завершения отсутствует. Конфигурация не изменялась; повторный запуск требует нового явного подтверждения.
- Вывод: после удаления WSOL fail-closed защиты закрытый CLI снова повторял цикл создания WSOL ATA в течение всего тестового окна и не дошёл до наблюдаемого арбитражного pipeline.

## 2026-07-24 — повторный upgrade до latest release

- Перед обновлением существующие пользовательские настройки, бинарник, upgrade script и state-файлы сохранены в mode-600 backup `backups/pre-upgrade-20260724T183843CEST`; `wallet.enc` в рабочем каталоге отсутствовал.
- `./upgrade.sh` успешно получил официальный latest release `v0.2.2` и завершил установку без ошибки.
- После установки основной бинарник и `upgrade.sh` побайтно идентичны pre-upgrade копиям; эффективного обновления версии не произошло.
- `config.toml`, `tokens.toml`, `state/CURRENT.md` и `state/EXPERIMENTS.md` не были изменены release-архивом. Права конфигурационных файлов остались `600`.
- После установки перечитаны локальные CLI `--version`/`--help`, `README.MD`, `HowToRun.MD` и официальные release notes. Release notes заявляют исправление automatic WSOL unwrapping, buffer после wrapping, balance check каждые 10 секунд, отключённый по умолчанию 3-hop и score-based sorting в auto mode.
- Ни одна transaction-capable команда не выполнялась.

## 2026-07-24 — минутный тест авторежима

- По утверждённому плану создан mode-600 backup wrapper; transaction-free RED/GREEN через подменный CLI подтвердил временную замену внутреннего timeout `300 → 60`.
- После свежего точного подтверждения выполнена ровно одна команда `./scripts/run-guarded.sh --live-confirmed`; повторной попытки не было.
- Guard завершил запуск по timeout: `60.153 s`, child exit `-2`.
- Баланс guard: `687379034 → 687329034` lamports; наблюдаемая дельта `-50000` lamports (`-0.00005 SOL`). Early stop `0.025 SOL` и loss target `0.03 SOL` не достигнуты.
- Mode-600 лог `logs/20260724T170038Z-zavod-cli.log`: `5` WSOL missing, `4` WSOL created, `0` transport, `0` route, `0` swap, `0` arb, `0` profit и `0` error/failed по агрегированному поиску.
- После попытки `scripts/run-guarded.sh` побайтно восстановлен из backup к штатному timeout `300`; временный fake CLI удалён, активный процесс отсутствует.
- Вывод: даже в минутном окне закрытый CLI повторяет WSOL ATA цикл и не показывает переход к арбитражному pipeline.

## 2026-07-24 — полная чистая переустановка

- До изменений создана внешняя mode-600 копия активного `config.toml`; совпадение с установленным конфигом проверено побайтно без вывода значений.
- Полное прежнее содержимое `/opt/zavod` упаковано в mode-600 `tar.gz`; целостность gzip и наличие ключевых записей истории/конфигурации проверены.
- Чистый checkout официального `master` установлен на commit `76ef44204c1cf9d47419dca44b2d9b7a0e19a701`; бинарник сообщает версию `0.2.2`.
- В новую установку возвращены только `config.toml`, `logs/`, `state/`, `docs/`, `runbooks/` и `AGENTS.md`; дополнительные пользовательские scripts/tests/backups/legacy оставлены только в архиве.
- Схема активного конфига сопоставлена с `config.toml.example` по именам секций и ключей: единственное отсутствующее новое поле — `extended_logs`; старых лишних ключей не найдено.
- Проверка ограничена Git provenance, SHA-256, документацией, `--version` и `--help`. Ни `run`, ни другая transaction-capable команда не выполнялась.

## 2026-07-24 — проверка чистой установки, остановлена оператором

- Мультиагентный read-only аудит подтвердил чистый upstream binary `0.2.2`; для запуска временно восстановлен архивный rollback guard без WSOL fail-closed по прямому указанию оператора.
- Исходный guard прошёл `23/23` unit-теста. Усиленная pre-rollback версия отдельно прошла `60/60` тестов, но для live-запуска не использовалась.
- Default preflight прошёл; выполнен один transaction-capable запуск с timeout `300 s`, early-stop `0.025 SOL` и loss target `0.03 SOL`.
- Оператор остановил тест через `127.649 s`: `reason=operator_signal`, `child_exit_code=-2`, наблюдаемая потеря `90000` lamports (`0.00009 SOL`).
- Лог `logs/20260724T173102Z-zavod-cli.log` имеет mode `600`: `9` WSOL missing, `8` WSOL created, событий route/swap/arb/profit и error/failed нет.
- После остановки ни одного CLI-процесса нет; конфигурация не изменена.
- Побайтное сравнение старой установки с чистой: `config.toml`, `tokens.toml` и основной binary идентичны. История показывает нормальный запуск CLI `0.2.2` 23 июля с распознанным WSOL account, затем замену primary RPC 24 июля и первый WSOL-цикл в ближайшем последующем запуске. Основная гипотеза — поведение нового RPC либо закрытого CLI при WSOL account lookup через этот RPC, а не переустановка.

## 2026-07-24 — read-only проверка вручную восстановленного RPC

- Перед проверкой оператор сообщил о возврате старого RPC; содержимое адреса и секретные значения не выводились.
- TOML parser отклонил активный конфиг: `Unclosed array (line 65, column 3)`.
- Безопасная структурная проекция выявила причину: массив `spam.sending_rpc_urls` открыт на строке 63, первый элемент на строке 64 не имеет запятой, последующие элементы и закрывающая `]` присутствуют.
- Primary `rpc.url` удалось извлечь без вывода значения; точного совпадения среди 13 parseable config-backup полного архива нет.
- Из-за невалидного TOML сетевой preflight и WSOL lookup не запускались. CLI-процесс отсутствует, транзакций не создавалось.

## 2026-07-24 — повторная read-only проверка после исправления TOML

- Между проверками оператор добавил отсутствующую запятую; повторный `tomllib` parse успешен, mode `config.toml` остался `600`.
- Read-only preflight прошёл: CLI `0.2.2`, loss target `30000000`, early-stop `25000000`, timeout `300`.
- Прямой `getTokenAccountsByOwner` текущего primary RPC для WSOL mint вернул один initialized account и raw amount `72423992`.
- Дополнительная проверка дала одинаковый результат на `processed`, `confirmed` и `finalized`: по одному account, initialized.
- Primary RPC функционально видит существующий WSOL account. Live-проверка закрытого CLI не выполнялась; транзакций и активных процессов нет.

## 2026-07-24 — live-проверка после возврата RPC

- После успешного preflight и свежего явного подтверждения выполнен один default live-запуск на `300 s` через архивный rollback guard без WSOL fail-closed.
- Guard result: `reason=timeout`, duration `300.232 s`, child exit `-2`, balance `687239034 → 684922035`, observed loss `2316999` lamports.
- В отличие от всех проблемных запусков 24 июля, CLI сразу сообщил `Payer WSOL account exists`; в полном логе `1 exists`, `0 missing`, `0 created`.
- Лог `logs/20260724T175403Z-zavod-cli.log` mode `600`, размер около `51 KiB`: `28` mint-list refresh, `3` LUT-события, `75` сообщения отправки, `0` WSOL loop и `0` error/failed.
- Finalized RPC-аудит точного окна нашёл `51` landed-транзакцию: `30` successful, `21` failed (`Custom 81`), aggregate fees `321304` lamports.
- Точная wallet SOL delta по finalized транзакциям `-2406681` lamports; расхождение с guard связано с последним балансом до окончательного landing. Разложение outflow: successful fees `211575`, failed fees `109729`, новый token-account rent `2074080`, executed transfers `11297`.
- wSOL delta `0`; settled SOL+wSOL delta `-0.002406681 SOL`. Большая часть outflow — rent нового token account, а не комиссия.
- После timeout CLI process group отсутствует. Повторного запуска не выполнялось.

## 2026-07-24 — single-mint auto runner implemented

- Added one-command single-mint preparation with strict mint/RPC validation, mode-600 snapshots, auto-mode isolation, and byte-exact restoration.
- Live execution remains gated by the exact immediate confirmation phrase and runs only through `scripts/run-guarded.sh`.
- Timeout is bounded to 30–300 seconds; early-stop `0.025 SOL` and loss target `0.03 SOL` are unchanged.
- Pool, route, and LUT discovery remain inside ZavodMevBot auto mode; static markets and new on-chain LUT creation are excluded.
- Transaction-free fake-process/mock-RPC tests cover preparation, refusal, cleanup, restoration, redaction, aggregation, and absence of retries.
- No transaction-capable command was executed during implementation verification.
